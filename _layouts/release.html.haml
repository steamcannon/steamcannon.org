---
layout: default
---

- release = release_for_version(page.version)

#right-sidebar
  #main-column
    .post
      .byline
        .author
          = page.author
        .date
          = page.date.strftime('%d %b %Y')
      .body
        ~ content
      .comments
        = page.intense_debate_comments
  #sidebar-column
    .post-navigation
      .previous
        - if page.previous_post
          .label
            previous
          %a{:href => page.previous_post.url}= page.previous_post.title
      .next
        - if page.next_post
          .label
            next
          %a{:href => page.next_post.url}= page.next_post.title
    .box
      %h1
        Release: #{release.version}
      %ul
        %li
          Amazon AMIs
          %br
          - release.amis.each do |ami|
            #{ami.ami_id} (#{ami.region})
            %br
        %li
          %a{:href => release.urls.dist.vmware} Download VMware Appliance
        %li
          %a{:href => release.urls.dist.zip} Download Zip
        %li
          %a{:href => release.urls.github.tree} Browse Sources
